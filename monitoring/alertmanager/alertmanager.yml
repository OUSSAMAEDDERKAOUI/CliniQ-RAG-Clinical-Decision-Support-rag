# route:
#   receiver: "default"
#   group_wait: 10s
#   group_interval: 5m
#   repeat_interval: 2h

# receivers:
# - name: "default"


global:
  smtp_smarthost: 'smtp.gmail.com:587'
  smtp_from: 'edderkaouioussama@gmail.com'
  smtp_auth_username: 'edderkaouioussama@gmail.com'
  smtp_auth_password: 'exuc hmgv cnhk lzvl'
  smtp_require_tls: true

route:
  receiver: "email-alert"

receivers:
- name: "email-alert"
  email_configs:
  - to: "fatihchaaban@gmail.com"
    send_resolved: true
    headers:
      subject: '[{{ .Status | toUpper }}] {{ .CommonAnnotations.summary }}'


